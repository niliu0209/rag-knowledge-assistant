"""隐私说明页（S2-3 公开部署上线必需）。

内容诚实陈述数据流向：上传文档与提问会发送给所选 LLM API（SiliconFlow
免费预设或用户自选提供商）用于解析/向量化/回答；Key 加密存储；邀请制范围。
纯静态展示（不调 API、不持有数据）；未登录也可访问。
"""

from __future__ import annotations

import streamlit as st

# 关键条款独立成常量：内容完整性由单测锁定（改文案先改测试）
PRIVACY_SECTIONS: list[tuple[str, str]] = [
    (
        "邀请制范围",
        "本服务采用邀请制：仅通过邀请码注册的用户可以使用，注册需提供用户名与密码。"
        "数据按账号隔离——每个账号只能看到和检索自己上传的文档。",
    ),
    (
        "数据流向（发送给 LLM API）",
        "你上传的文档与你提出的问题，会发送给你选择的 LLM API 提供商（默认平台免费预设"
        "使用的 SiliconFlow，或你自己配置的其他兼容提供商）进行解析、向量化与回答生成。"
        "**即：文档内容会离开本服务器，发送给第三方 API 服务。** 请勿上传你不想"
        "发送给第三方的敏感内容（如密码、证件、密钥等）。",
    ),
    (
        "API Key 存储",
        "如果你配置了自己的 API Key，它使用主密钥加密后存储在服务器数据库中，"
        "不会明文保存；平台共享 Key 仅由管理员在服务器环境变量中配置，不写入数据库、"
        "日志或界面。加密密钥与数据分离管理（服务器环境变量注入）。",
    ),
    (
        "平台免费预设",
        "不配置自己 Key 的用户默认使用平台共享额度（SiliconFlow 免费预设）。"
        "共享额度可能受提供商免费政策影响（限流/失效），届时可改为自带 Key。",
    ),
    (
        "账号与会话安全",
        "密码使用 Argon2id 哈希存储（不存明文）；会话使用 HttpOnly/Secure cookie，"
        "管理员可随时停用账号（停用即时失效）。",
    ),
    (
        "服务托管与存储",
        "服务部署于中国大陆服务器（阿里云），你的账号、文档与问答记录存储在该服务器"
        "磁盘上。服务器与域名信息见页面底部备案号。",
    ),
    (
        "删除你的数据",
        "你可以删除自己上传的文档；需要删除账号与全部记录时，请联系管理员处理。",
    ),
]


def render_privacy_page() -> None:
    st.title("隐私说明")
    st.caption("公开部署服务（邀请制）——请在使用前阅读")
    for title, body in PRIVACY_SECTIONS:
        st.markdown(f"#### {title}")
        st.markdown(body)
